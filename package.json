{
  "name": "telegram-bot-proxy",
  "description": "A Cloudflare Worker that acts as a transparent proxy for the Telegram Bot API. Useful for bypassing network restrictions or as a middleware for your Telegram bot.",
  "cloudflare": {
    "label": "telegram-bot-proxy",
    "products": [
      "Workers"
    ],
    "categories": [],
    "publish": true
  },
  "dependencies": {
  },
  "devDependencies": {
    "wrangler": "4.21.x"
  },
  "scripts": {
    "dev": "wrangler dev --port 8080",
    "build": "wrangler build",
    "publish": "wrangler publish"
  }
}